import { categoryColor } from 'kiosk/helpers/category-color';
import { module, test } from 'qunit';

module('Unit | Helper | category color');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = categoryColor([42]);
  assert.ok(result);
});
